# Journal

Journal entries are concise, human-readable, Git-tracked records of architecture
decisions, experiments, bugs, reviews, and project evolution.

They are not runtime logs, credentials, API responses, or an automatic event dump.
Runtime diagnostics belong under `log/runtime/` and are ignored by Git. Never put
API keys, tokens, passwords, or secret file contents in a journal entry.
