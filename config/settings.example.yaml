llm:
  provider: deepseek
  model: deepseek-chat
  endpoint: https://api.deepseek.com
  credential:
    source: env
    env_name: DEEPSEEK_API_KEY

paths:
  workspace: workspace
  runtime_data: data/runtime
  log: log/runtime
