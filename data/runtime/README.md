# Runtime data

This directory is reserved for local checkpoint databases, caches, and other
machine-generated runtime data. Concrete databases are ignored by Git; this README
documents the boundary only.
